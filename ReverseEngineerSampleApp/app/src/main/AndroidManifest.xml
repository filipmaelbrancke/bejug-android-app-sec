<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.appfoundry.android.bejug" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="be.appfoundry.android.bejug.PersonListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="be.appfoundry.android.bejug.PersonDetailActivity"
            android:label="@string/title_person_detail"
            android:parentActivityName="be.appfoundry.android.bejug.PersonListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="be.appfoundry.android.bejug.PersonListActivity" />
        </activity>
    </application>

</manifest>
